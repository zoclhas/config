local options = {
  ensure_installed = { "lua", "python", "typescript", "javascript", "rust" },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
